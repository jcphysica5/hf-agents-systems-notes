name: hf-agents-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - jupyter
  - pip:
      - mkdocs>=1.6
      - mkdocs-material>=9.5
      - pymdown-extensions>=10.7
      - pygments>=2.17
      - smolagents>=1.0
      - huggingface-hub>=0.24
      - transformers>=4.40
      - datasets>=2.20
      - torch>=2.2
      - langchain>=0.2
      - langgraph>=0.1
      - llama-index>=0.10
      - python-dotenv>=1.0
